-tcp is lowlevel and we have to worry about individual packets..
-but http is application level..
-HTTPX allows Python to send HTTP requests.

-request: Request

-FastAPI gives us the original request:
It contains:HTTP Method,Headers,Body,Query Parameters,Cookies ,Client IP

-in lifetime function,everything after yield runs when the application/service is shutting down

-round robin load balancing give tasks for each instance in an order.
-least connection load balancing track active requests for each instance and assigns task to those instance with less active connections
-Find all instances with the minimum number of connections(Least connections method).then,If several instances tie, distribute requests among them using Round Robin.


-Prometheus is an open-source monitoring and alerting system widely used in cloud-native environments.
-Prometheus keep track of all the metrics used in cloud traffic monitoring

-Grafana for visualizing,displaying ,dashboards,alerts..It simply asks Prometheus

-Latency is simply:How long did it take to process one request?

-"Why is this called a Cloud Traffic Manager instead of an API Gateway?"
    You can answer:"Besides routing traffic, it continuously monitors load using Prometheus metrics, makes scaling decisions based on configurable thresholds, and dynamically updates the service registry. This simulates cloud-native autoscaling while integrating with health monitoring, rate limiting, and circuit breaking."

-each server gets its own docker file