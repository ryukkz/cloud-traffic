-tcp is lowlevel and we have to worry about individual packets..
-but http is application level..
-HTTPX allows Python to send HTTP requests.

-request: Request

-FastAPI gives us the original request:
It contains:HTTP Method,Headers,Body,Query Parameters,Cookies ,Client IP

-in lifetime function,everything after yield runs when the application/service is shutting down

-round robin load balancing give tasks for each instance in an order.
-least connection load balancing track active requests for each instance and assigns task to those instance with less active connections
-Find all instances with the minimum number of connections(Least connections method).then,If several instances tie, distribute requests among them using Round Robin.
